import { createMetadata } from '@/lib/seo'; import { comparisonRows } from '@/lib/data'; import { SectionHeading } from '@/components/section-heading'; export const metadata = createMetadata({ title: 'مقارنة المنصات', description: 'مقارنة بين Booking وAgoda وSkyscanner وWego للمستخدم العربي.', path: '/comparison' }); export default function ComparisonPage() { return <section className="mx-auto max-w-7xl px-4 py-16 sm:px-6 lg:px-8"><SectionHeading title="مقارنة منصات الحجز" description="اعرف المنصة الأنسب للفنادق أو الرحلات أو العروض العربية قبل الضغط على رابط الحجز." /><div className="overflow-hidden rounded-[2rem] border border-slate-200 bg-white shadow-soft"><div className="overflow-x-auto"><table className="min-w-full text-right text-sm"><thead className="bg-slate-50 text-ink"><tr>{['المنصة','الأفضل لـ','نقاط القوة','ملاحظات','ملاءمة الأفلييت'].map((head) => <th key={head} className="px-5 py-4 font-black">{head}</th>)}</tr></thead><tbody>{comparisonRows.map((row) => <tr key={row.platform} className="border-t border-slate-100"><td className="px-5 py-4 font-black text-ink">{row.platform}</td><td className="px-5 py-4 text-slate-600">{row.bestFor}</td><td className="px-5 py-4 text-slate-600">{row.strengths}</td><td className="px-5 py-4 text-slate-600">{row.weakness}</td><td className="px-5 py-4 text-slate-600">{row.commissionHint}</td></tr>)}</tbody></table></div></div></section>; }