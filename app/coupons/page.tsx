import { createMetadata } from '@/lib/seo'; import { coupons } from '@/lib/data'; import { CouponCard } from '@/components/coupon-card'; import { SectionHeading } from '@/components/section-heading'; export const metadata = createMetadata({ title: 'الكوبونات', description: 'أكواد خصم عربية على منصات الحجز والسفر والفنادق.', path: '/coupons' }); export default function CouponsPage() { return <section className="mx-auto max-w-7xl px-4 py-16 sm:px-6 lg:px-8"><SectionHeading title="كوبونات السفر" description="أكواد خصم قابلة للنسخ بسرعة مع تاريخ الانتهاء والمتجر وقيمة التخفيض." /><div className="grid gap-6 md:grid-cols-2 xl:grid-cols-3">{coupons.map((coupon) => <CouponCard key={coupon.code} coupon={coupon} />)}</div></section>; }