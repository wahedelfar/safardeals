import Link from 'next/link'; import { createMetadata } from '@/lib/seo'; import { destinations } from '@/lib/data'; import { SectionHeading } from '@/components/section-heading'; export const metadata = createMetadata({ title: 'الوجهات', description: 'أدلة عربية مختصرة لأفضل الوجهات من حيث التكلفة والموسم والنصائح.', path: '/destinations' }); export default function DestinationsPage() { return <section className="mx-auto max-w-7xl px-4 py-16 sm:px-6 lg:px-8"><SectionHeading title="الوجهات" description="أدلة عملية إلى دبي وتركيا والسعودية وأوروبا والمالديف مع أفضل وقت للزيارة ومتوسط التكلفة." /><div className="grid gap-6 md:grid-cols-2 xl:grid-cols-3">{destinations.map((destination) => <Link key={destination.slug} href={`/destinations/${destination.slug}`} className="rounded-[2rem] border border-slate-200 bg-white p-6 shadow-soft transition hover:-translate-y-1"><div className="text-xs font-bold text-sky-600">{destination.bestTime}</div><h2 className="mt-3 text-2xl font-black text-ink">{destination.name}</h2><p className="mt-3 text-sm leading-7 text-slate-600">{destination.intro}</p><p className="mt-4 text-sm font-semibold text-slate-700">متوسط المصاريف: {destination.averageCost}</p></Link>)}</div></section>; }