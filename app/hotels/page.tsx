import { createMetadata } from '@/lib/seo'; import { hotelDeals } from '@/lib/data'; import { DealCard } from '@/components/deal-card'; import { SectionHeading } from '@/components/section-heading'; export const metadata = createMetadata({ title: 'عروض الفنادق', description: 'أفضل عروض الفنادق والإقامة الاقتصادية والمنتجعات للمسافرين العرب.', path: '/hotels' }); export default function HotelsPage() { return <section className="mx-auto max-w-7xl px-4 py-16 sm:px-6 lg:px-8"><SectionHeading title="عروض الفنادق" description="بطاقات فنادق واضحة مع السعر والتقييم والمزود وروابط الحجز السريعة." /><div className="mb-8 flex flex-wrap gap-3">{['اقتصادي','عائلي','فاخر','شهر العسل','وسط المدينة'].map((filter) => <span key={filter} className="rounded-full border border-slate-200 bg-white px-4 py-2 text-sm font-semibold text-slate-600">{filter}</span>)}</div><div className="grid gap-6 md:grid-cols-2 xl:grid-cols-3">{hotelDeals.map((deal) => <DealCard key={deal.id} deal={deal} />)}</div></section>; }