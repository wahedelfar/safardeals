import ReactMarkdown from 'react-markdown'; import remarkGfm from 'remark-gfm'; import remarkSlug from 'remark-slug'; import { notFound } from 'next/navigation'; import { FAQ } from '@/components/faq'; import { getAllPosts, getPostBySlug, getRelatedPosts } from '@/lib/blog'; import { createMetadata, faqSchema } from '@/lib/seo'; import Link from 'next/link'; export async function generateStaticParams() { return getAllPosts().map((post) => ({ slug: post.slug })); } export async function generateMetadata({ params }: { params: { slug: string } }) { const post = getPostBySlug(params.slug); if (!post) return {}; return createMetadata({ title: post.title, description: post.excerpt, path: `/blog/${post.slug}`, keywords: post.tags }); } function buildToc(content: string) { return content.split('
').filter((line) => line.startsWith('## ')).map((line) => ({ text: line.replace('## ', ''), slug: line.replace('## ', '').trim().toLowerCase().replace(/\s+/g, '-') })); } export default function BlogPostPage({ params }: { params: { slug: string } }) { const post = getPostBySlug(params.slug); if (!post) notFound(); const related = getRelatedPosts(post.slug, post.category); const toc = buildToc(post.content); const schema = faqSchema(post.faqs); return <section className="mx-auto grid max-w-7xl gap-8 px-4 py-16 sm:px-6 lg:grid-cols-[260px_1fr] lg:px-8"><aside className="h-max rounded-[2rem] border border-slate-200 bg-white p-5 shadow-soft lg:sticky lg:top-24"><div className="text-sm font-black text-ink">فهرس المقال</div><div className="mt-4 flex flex-col gap-3 text-sm text-slate-600">{toc.map((item) => <a key={item.slug} href={`#${item.slug}`} className="transition hover:text-sky-600">{item.text}</a>)}</div></aside><article className="rounded-[2rem] border border-slate-200 bg-white p-6 shadow-soft sm:p-8"><script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(schema) }} /><div className="text-sm font-bold text-sky-600">{post.category}</div><h1 className="mt-4 text-4xl font-black leading-tight text-ink">{post.title}</h1><p className="mt-4 text-base leading-8 text-slate-600">{post.excerpt}</p><div className="mt-3 text-xs text-slate-500">{post.date} • {post.readingMinutes}</div><div className="prose prose-slate mt-10 max-w-none prose-headings:font-black prose-a:text-sky-600 prose-p:leading-8"><ReactMarkdown remarkPlugins={[remarkGfm, remarkSlug]}>{post.content}</ReactMarkdown></div><div className="mt-12"><h2 className="text-2xl font-black text-ink">الأسئلة الشائعة</h2><div className="mt-5"><FAQ items={post.faqs} /></div></div><div className="mt-12"><h2 className="text-2xl font-black text-ink">مقالات مرتبطة</h2><div className="mt-5 grid gap-4 md:grid-cols-3">{related.map((item) => <Link key={item.slug} href={`/blog/${item.slug}`} className="rounded-3xl bg-slate-50 p-4 text-sm font-semibold text-slate-700 transition hover:text-sky-600">{item.title}</Link>)}</div></div></article></section>; }