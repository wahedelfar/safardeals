import { createMetadata } from '@/lib/seo'; import { getAllPosts } from '@/lib/blog'; import { BlogCard } from '@/components/blog-card'; import { SectionHeading } from '@/components/section-heading'; export const metadata = createMetadata({ title: 'المدونة', description: 'مدونة عربية حول السفر الاقتصادي ونصائح الحجز والفنادق والفيزا وشهر العسل.', path: '/blog' }); export default function BlogPage() { const posts = getAllPosts(); return <section className="mx-auto max-w-7xl px-4 py-16 sm:px-6 lg:px-8"><SectionHeading title="مدونة السفر" description="محتوى عربي منظم حسب الفئات: السفر الاقتصادي، النصائح، الفنادق، الرحلات، الفيزا، وشهر العسل." /><div className="mb-8 flex flex-wrap gap-3">{['Budget Travel','Travel Tips','Hotels','Flights','Visa Guides','Honeymoon'].map((category) => <span key={category} className="rounded-full border border-slate-200 bg-white px-4 py-2 text-sm font-semibold text-slate-600">{category}</span>)}</div><div className="grid gap-6 md:grid-cols-2 xl:grid-cols-3">{posts.map((post) => <BlogCard key={post.slug} post={post} />)}</div></section>; }