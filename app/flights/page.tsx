import { createMetadata } from '@/lib/seo'; import { flightDeals } from '@/lib/data'; import { DealCard } from '@/components/deal-card'; import { SectionHeading } from '@/components/section-heading'; export const metadata = createMetadata({ title: 'عروض الرحلات', description: 'أفضل عروض الرحلات الجوية الرخيصة للمسافرين من مصر والسعودية والإمارات.', path: '/flights' }); export default function FlightsPage() { return <section className="mx-auto max-w-7xl px-4 py-16 sm:px-6 lg:px-8"><SectionHeading title="عروض الرحلات الجوية" description="فلترة ذكية حسب الوجهة والمزود والسعر مع روابط حجز أفلييت مباشرة." /><div className="mb-8 flex flex-wrap gap-3">{['دبي','تركيا','السعودية','أوروبا','المالديف'].map((filter) => <span key={filter} className="rounded-full border border-slate-200 bg-white px-4 py-2 text-sm font-semibold text-slate-600">{filter}</span>)}</div><div className="grid gap-6 md:grid-cols-2 xl:grid-cols-3">{flightDeals.map((deal) => <DealCard key={deal.id} deal={deal} />)}</div></section>; }